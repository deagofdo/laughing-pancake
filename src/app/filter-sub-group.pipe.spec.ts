import { FilterSubGroupPipe } from './filter-sub-group.pipe';

describe('FilterSubGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSubGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
